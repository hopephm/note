#include<iostream>

using namespace std;

int main() {
	int k;
	int M, N, X, Y;

	cin >> k;

	for (int i = 0; i < k; i++) {
		cin >> M >> N >> X >> Y;

		if (X<1 || X>M || Y<1 || X>N || M<1 || N<1 || M > 40000 || N > 40000) {
			printf("%d",-1);
		}
		else {

		}


	}
}